<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Split"
        tools:targetApi="31" >
        <meta-data
            android:name="com.google.firebase.auth.recaptcha.SITE_KEY"
            android:value="6Lfqa1clAAAAAD3y-rr2hQ_3a7KRY-nkkccOYoLL" />
        <activity
            android:name=".loginSignup.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".newExpense.SelectPayerActivity"
            android:exported="false" />
        <activity
            android:name=".newExpense.SelectParticipantsActivity"
            android:exported="false" />
        <activity
            android:name=".NewExpenseActivity"
            android:theme="@style/Theme.Split.NoActionBar"
            android:exported="false" />
        <activity
            android:name=".loginSignup.SignupActivity"
            android:label="@string/signup_page_name"
            android:exported="false"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

    </application>

</manifest>